services:
  zy-stats:
    build:
      context: .
      pull: true
    environment:
      - FORCE_COLOR=1
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    # restart: unless-stopped
